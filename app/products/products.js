import React from "react";
products = {
  // src={product.image}
  // alt={product.name}
  // alt={product.description}
};

const page = () => {
  return <div>Products page</div>;
};

export default page;
